/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:'cybersecqube.com'
            }
        ]
    }
};

export default nextConfig;
